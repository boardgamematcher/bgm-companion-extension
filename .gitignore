# Browser extension artifacts
*.crx
*.xpi
*.zip

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo

# Node modules (if we add build step later)
node_modules/

# Local Claude Code session state (scheduled tasks, worktrees, settings)
.claude/

# Local secrets (BGM-789 spike — never commit BGA credentials)
.env
.env.local
